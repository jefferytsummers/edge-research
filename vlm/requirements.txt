# VLM Container Dependencies
redis>=5.0.0
pydantic>=2.5.0

# VLM inference (installed in dustynv/nano_llm container)
# nano_llm  # Provided by container base image

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
